basically when you merge, it creates a **separate merge commit**. The Merge Commit has 2 parents

generally

```git
merge fix1 to master
```

Sometimes git could create what is called a fast forward merge

